version: '3.8'

services:
  # Наша база данных (Redis)
  redis-db:
    image: redis:alpine
    ports:
      - "6379:6379"

  # Голосовалка (веб-приложение)
  voting-app:
    image: dockersamples/examplevotingapp_vote
    ports:
      - "5000:80"
    depends_on:
      - redis-db